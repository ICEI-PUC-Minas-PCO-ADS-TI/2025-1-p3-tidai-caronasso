@model caronasso.Models.Chat

@{
    ViewData["Title"] = "Create";
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,1,0" />

    <title>Chats - Confirmação</title>
</head>
<body>
    <main>
        <h1>Confirmação Chat</h1>
        
        <form asp-action="CreateChat" asp-route-destinatarioId="@ViewBag.DestinatarioId">
            <img class="rounded" width="200px" src="@ViewBag.DestinatarioFotoPerfil" alt="Imagem Destinatário" />
            <h2>@ViewBag.DestinatarioNome</h2>
            <button type="submit" value="CreateChat">Iniciar Chat</button>
        </form>
    </main>
    <script src="~/assets/js/chat.js"></script>

    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        img {
            display: block;
            max-width: 100%;
        }
        main {
            max-width: 960px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
           & form {
               display: flex;
               flex-direction: column;
               padding: 75px 150px;
               background-color: #004aad;
               border-radius: 5px;
               margin-top: 50px;
               align-items: center;

               & h2 {
                   color: white;
               }

               & button {
                   border: none;
                   border-radius: 5px;
                   padding: 10px 30px;
                   color: #004aad;
                   background: lightgray;
               }
               & button:hover {
                   color: blue;
                   background-color: white;
                   transition: background-color 0.5s ease-in-out;
               }
           }
        }
        
    </style>
</body>
</html>
